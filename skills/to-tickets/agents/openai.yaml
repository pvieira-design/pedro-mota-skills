interface:
  display_name: "To Tickets"
  short_description: "Split approved work into blocked tracer tickets"
  default_prompt: "Use $to-tickets to decompose the approved plan into executable tracer tickets."

policy:
  allow_implicit_invocation: false
