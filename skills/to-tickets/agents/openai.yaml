interface:
  display_name: "To Tickets"
  short_description: "Publish executable children from a spec"
  default_prompt: "Use $to-tickets to decompose the approved GitHub spec into executable child issues."

policy:
  allow_implicit_invocation: true
