# <Component/feature name>

## Last updated: YYYY-MM-DD — <real reason for the change>

## What it is

<2-3 lines: what this component/feature does and why it exists>

## Where it lives

<List the repo's real layers. Example labels — adapt to the project's layout:>

- Frontend (screen/route): `<path>`
- Backend (handler/route): `<path>`
- Worker/queue/job: `<path>`
- Schema/persistence: `<path>`

## API / procedures / routes

- `name.of.handler(input)` — auth: `<public|session|role>` — description

## Business rules (non-obvious)

- <TTLs, limits, validations, execution order, quirks the code doesn't reveal on its own>

## External dependencies / env

- <third-party APIs, environment variables, integrations>

## Key files

- `path/to/file.ext` — role
