# {Context / product name}

{One or two sentences: what this context is and why it exists.}

> **This file is a GLOSSARY — and nothing else.** It defines the domain's canonical vocabulary: what each term means in this project. It is not a spec, not implementation docs, not a scratchpad. "How the code does it" lives in `docs/system/`; "what we're going to do" in `docs/plans/`; "why we decided" in `docs/adr/`. Here only the *meaning of the words* lives.
>
> Rules: be opinionated (when synonyms exist, pick one and list the rest under `_Avoid_`); tight definitions (1-2 sentences, what the thing IS, not what it does); only terms specific to this domain (general programming concepts don't belong). Maintained during grilling (`/grill-with-docs`).

## Language

<!-- Format example — replace with the domain's real terms:

**Order**:
A customer's purchase request.
_Avoid_: Purchase, transaction

**Customer**:
A person or organization that places orders.
_Avoid_: Buyer, account, user
-->

_(no terms yet — add them as the domain vocabulary gets pinned down, ideally during a `/grill-with-docs` session.)_
