interface:
  display_name: "Implement"
  short_description: "Implement one executable child issue"
  default_prompt: "Use $implement on the referenced executable child issue in a clean session."

policy:
  allow_implicit_invocation: false
