interface:
  display_name: "Grill With Docs"
  short_description: "Close decisions in a tracker-backed grill"
  default_prompt: "Use $grill-with-docs to close this bounded decision work in a compaction-safe GitHub grill issue."

policy:
  allow_implicit_invocation: true
