interface:
  display_name: "Grill With Docs"
  short_description: "Interrogate plans and update domain docs"
  default_prompt: "Use $grill-with-docs to close the decisions in this plan without implementing it."

policy:
  allow_implicit_invocation: false
