interface:
  display_name: "To Spec"
  short_description: "Promote a completed decision trail to spec"
  default_prompt: "Use $to-spec on the completed grill or Wayfinder issue to publish the non-executable parent spec."

policy:
  allow_implicit_invocation: false
