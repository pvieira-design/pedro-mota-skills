interface:
  display_name: "To Spec"
  short_description: "Publish decision-complete work as a spec"
  default_prompt: "Use $to-spec to publish this decision-complete brief, completed grill, or resolved Wayfinder map as a non-executable parent spec."

policy:
  allow_implicit_invocation: true
